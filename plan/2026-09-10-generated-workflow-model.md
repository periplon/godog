# Generated workflow model

Make the selected model explicit in generated YAML for every implementation and review task, while retaining the workflow default.

1. Reproduce the installed CLI output and inspect compiler inheritance.
2. Add a regression that inspects generated YAML before compilation; confirm it fails.
3. Populate generated task models, then run focused and broader checks.
4. Review serialization and runtime compatibility; commit and open a draft PR.

Implementation and local verification complete. Delivery: focused branch `jmca/generated-workflow-model`; prepare draft PR. Installed binary remains unchanged pending delivery.
